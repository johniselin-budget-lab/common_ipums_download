# =============================================================================
# parameters.yaml  —  the ONE file you edit to define a common IPUMS extract.
#
# download_ipums.R reads this file, builds an IPUMS extract, downloads it to the
# shared raw_data area, and writes a manifest describing what it pulled.
#
# The variable list below is the COMMON ACS variable set: the union of the ACS
# variables that existing Budget Lab project extracts request from IPUMS USA,
# organized into labeled groups. It is a *starting point* — add/remove variables
# here, don't fork the program.
#
# Setup once:
#   cp config/api_codes.example.csv config/api_codes.csv   # then paste your key
# Then:  module load R  &&  Rscript download_ipums.R
# =============================================================================

# ---- Collection -------------------------------------------------------------
# "usa" = IPUMS USA (ACS / decennial).  "cps" = IPUMS CPS (see
# config/parameters.cps.example.yaml). The program is collection-agnostic: to
# pull CPS someday, point download_ipums.R at a CPS parameter file instead.
collection: usa

# Free-text description attached to the extract on the IPUMS side and copied
# into the manifest. Keep it identifying — it's how you'll spot the pull later.
description: "Budget Lab common ACS extract (shared raw_data source)"

# ---- Samples ----------------------------------------------------------------
# IPUMS USA sample IDs, one per ACS 1-year file, convention usYYYYa.
# NOTE ON AVAILABILITY: IPUMS releases each new ACS 1-year sample in early winter
# after the survey year (us2024a ~ Dec/Jan). Requesting an unreleased sample is a
# hard error. us2020a is the EXPERIMENTAL 2020 ACS 1-year file (the standard 2020
# 1-year release was cancelled due to COVID data-collection disruptions); it is
# included below. For 2020, IPUMS supplies the experimental (COVID-adjusted)
# weights under BOTH the standard names HHWT/PERWT and the experimental names
# EXPWTH/EXPWTP — so pooling on PERWT works; the 2020 weights are just experimental.
# Check availability: https://usa.ipums.org/usa-action/samples
samples:
  # ACS 1-year files back to 2006. Earlier years carry fewer variables (the SPM
  # block, WKSWORK1, several income sources, etc. phase in over time); the
  # per-sample availability pruning drops whatever a given year doesn't offer and
  # records it in that year's manifest. The ACS 1-year series starts in 2005.
  - us2006a
  - us2007a
  - us2008a
  - us2009a
  - us2010a
  - us2011a
  - us2012a
  - us2013a
  - us2014a
  - us2015a
  - us2016a
  - us2017a
  - us2018a
  - us2019a
  - us2020a
  - us2021a
  - us2022a
  - us2023a
  - us2024a
  # NOTE: us2020a is the experimental COVID-year 1-year file. For 2020, IPUMS
  # stores the experimental weights under BOTH HHWT/PERWT and EXPWTH/EXPWTP (below).
  # us2005a (the first ACS 1-year) could be added; older years carry progressively
  # fewer variables (health insurance from 2008, SPM block from ~2009, etc.) — the
  # per-sample availability pruning handles the gaps and logs them per year.
  #
  # ACS 5-YEAR file (convention usYYYYc, ending year in the ID). Most recent
  # release: us2024c = the 2020-2024 ACS 5-year. Note this overlaps the 1-year
  # files above (2020-2024), so DO NOT pool it with the 1-year samples for
  # weighted totals — you would double-count those person-years. It lives in its
  # own per_year folder (us2024c/) and is meant to be read on its own for
  # small-geography (e.g. PUMA/county) estimates.
  - us2024c

# ---- Variables --------------------------------------------------------------
# Plain variable names, grouped by topic. Per-variable options (data-quality
# flags, case selections) are set in the two blocks AFTER this list. Trailing
# comments describe non-obvious variables; where a comment says "(dq flag below)"
# the variable is also listed under data_quality_flags.
variables:
  # --- Record identifiers & survey design weights ---------------------------
  - YEAR
  - SAMPLE
  - SERIAL          # household serial
  - CBSERIAL        # Census Bureau original household serial
  - PERNUM
  - HHWT            # household weight
  - PERWT           # person weight
  - EXPWTH          # experimental household weight (2020 ACS experimental sample)
  - EXPWTP          # experimental person weight (2020 ACS experimental sample)
  - CLUSTER         # variance-estimation cluster
  - STRATA          # variance-estimation stratum
  - CPI99           # CPI-U inflation adjustment factor to 1999$
  # Multi-year-sample support. Both are 5-year-file concepts and exist only for
  # us2024c; the per-sample pruning drops them from the nineteen 1-year samples
  # and logs it, the same known asymmetry EXPWTH/EXPWTP have for us2020a.
  - MULTYEAR        # actual survey year within a multi-year sample
  - ADJUST          # income adjustment factor for multi-year samples

  # --- Geography ------------------------------------------------------------
  - REGION
  - STATEFIP
  - COUNTYFIP       # only for fully-identified counties (partial coverage)
  - PUMA

  # --- Household structure & family relationships ---------------------------
  - GQ              # group-quarters status (NOT filtered here; see case_selections)
  - RELATE
  - RELATED
  - MARST
  - NCHILD
  - NCHLT5          # number of own children under 5
  - YNGCH
  - SPLOC
  - MOMLOC
  - POPLOC
  - MOMLOC2
  - POPLOC2

  # --- Demographics ---------------------------------------------------------
  - AGE
  - SEX
  - RACE
  - RACED           # detailed race
  - HISPAN
  - HISPAND         # detailed Hispanic origin
  - CITIZEN
  - SCHOOL
  - EDUC
  - EDUCD           # detailed education
  # Enrollment LEVEL, folded in from the shelter layer 2026-09-04. SCHOOL says
  # whether someone is enrolled; GRADEATT says in what — the only thing that
  # separates a college student from a high-school student, which the SNAP
  # student rule turns on. SCHLTYPE adds public vs private (the school-meals
  # offset); FERTYR is a birth in the last year (the WIC offset).
  - GRADEATT
  - GRADEATTD       # detailed grade attending
  - SCHLTYPE
  - FERTYR

  # --- Employment & labor supply --------------------------------------------
  - LABFORCE
  - EMPSTAT         # (dq flag below)
  - EMPSTATD        # (dq flag below)
  - CLASSWKR        # (dq flag below)
  - WORKEDYR        # (dq flag below)
  - WKSWORK1        # weeks worked last year, intervalled
  - WKSWORK2        # weeks worked last year, intervalled (dq flag below)
  - UHRSWORK        # usual hours worked/week (dq flag below)

  # --- Income & program participation (most carry dq flags below) -----------
  - HHINCOME        # total household income
  - FTOTINC         # total family income
  - INCTOT          # total personal income
  - INCWAGE
  - INCEARN
  - INCBUS00
  - INCINVST
  - INCSS           # Social Security income
  - INCWELFR        # welfare (public assistance) income
  - INCSUPP         # supplemental security income
  - INCRETIR        # retirement income
  - INCOTHER
  - FOODSTMP        # SNAP receipt (dq flag below)

  # --- Health insurance -----------------------------------------------------
  - HCOVANY
  - HCOVPRIV
  - HINSEMP
  - HINSCAID
  - HINSCARE

  # --- Housing / tenure (dq flags below) ------------------------------------
  # The shelter and dwelling variables below were folded in from the
  # acs_shelter_1yr_v2 and acs_housing merge layers on 2026-09-04
  # (docs/consolidation_plan.md D1). Availability was probed against the live
  # API first: us2006a carries every one of them, so the fold reaches back
  # across the whole panel rather than only the recent years.
  - OWNERSHP
  - RENT            # monthly CONTRACT rent (excludes utilities)
  - VALUEH          # house value
  - PROPTX99        # property taxes, bracketed (PROPTXAMT is not a valid IPUMS
                    # USA name — the API rejects it; OWNCOST includes property
                    # tax anyway, so nothing is lost)
  # The two shelter-cost concepts a means-tested model actually needs. SNAP's
  # excess-shelter deduction takes gross rent for renters and, by statute,
  # selected monthly owner costs for owners; OWNERSHP is what decides which
  # applies. Without these a SNAP model deducts contract rent for renters and
  # nothing at all for owners — roughly two thirds of households.
  - RENTGRS         # gross rent = contract rent + utilities
  - OWNCOST         # selected monthly owner costs (SMOC)
  # Owner-cost components. OWNCOST is a sum, so these are what lets a model
  # rebuild it — or substitute a Standard Utility Allowance for the utility part.
  - MORTGAGE        # mortgage status: owned free and clear vs mortgaged
  - MORTAMT1        # first mortgage monthly payment
  - MORTAMT2        # second mortgage / HELOC monthly payment
  - TAXINCL         # does MORTAMT1 include property taxes?
  - INSINCL         # does MORTAMT1 include property insurance?
  - PROPINSR        # annual property insurance
  - CONDOFEE        # monthly condominium fee
  - MOBLHOME        # annual mobile-home costs
  # Utilities. Needed on their own because RENTGRS and OWNCOST both embed
  # ACTUAL utility bills, and a Standard Utility Allowance treatment has to
  # strip them back out. FUELHEAT matters because the allowance varies by
  # heating source.
  - COSTELEC
  - COSTGAS
  - COSTWATR
  - COSTFUEL
  - FUELHEAT
  # Dwelling characteristics, from the acs_housing layer (the Housing-Utilities
  # boundary and the FMR rent surface).
  - ROOMS
  - BEDROOMS
  - UNITSSTR        # structure type
  - BUILTYR2        # year structure built
  - MOVEDIN         # year householder moved in
  - KITCHEN
  - PLUMBING
  - ACREHOUS        # house-and-lot acreage
  - RENTMEAL        # are meals included in rent?
  - VEHICLES

  # --- SPM (Supplemental Poverty Measure) block -----------------------------
  # Census/NBER-TAXSIM-derived. The full block below is the union needed across
  # downstream projects (esp. the Affordability index, which rebuilds SPM
  # resources). Many components have limited year availability (SPM starts ~2009;
  # some tax/credit fields later still) — the per-sample availability pruning
  # drops what a given year lacks and logs it in that year's manifest.
  # Unit, threshold, resources, poverty status:
  - SPMFAMUNIT
  - SPMNPERS
  - SPMNADULTS
  - SPMNCHILD
  - SPMCOHABIT       # cohabiting-partner presence in SPM unit
  - SPMUICHILD       # unrelated individuals <15 in SPM unit
  - SPMEQSCALE       # SPM equivalence scale
  - SPMGEOADJ        # SPM geographic adjustment
  - SPMTHRESH        # SPM poverty threshold
  - SPMTOTRES        # SPM unit resources (total)
  - SPMFTOTVAL       # SPM unit pre-tax money income
  - SPMPOV           # SPM poverty status
  - OFFPOV           # official poverty status
  # Resource add-on components (in-kind benefits):
  - SPMSNAP          # SNAP value
  - SPMCAPHOUS       # housing subsidy / capped rent
  - SPMLUNCH         # school lunch value
  - SPMHEAT          # LIHEAP / energy assistance
  - SPMWIC           # WIC value
  - SPMMORT          # on-mortgage flag (threshold selection)
  # Expense components (subtracted from resources):
  - SPMCAPXPNS       # capped work + child care expenses
  - SPMWKXPNS        # work expenses
  - SPMCHXPNS        # child care expenses
  - SPMMEDXPNS       # medical out-of-pocket (SPM MOOP)
  - MOOP             # medical out-of-pocket (person)
  - MEDICAREB        # Medicare Part B premium
  - SPMPREMIUM       # health insurance premiums
  # Taxes & credits (TAXSIM-derived):
  - TAXID
  - ADJGINC
  - SPMFEDTAXAC      # federal tax after credits
  - SPMFEDTAXBC      # federal tax before credits
  - SPMSTTAX         # state tax
  - SPMEITC          # federal EITC
  - SPMFICA          # payroll (FICA) tax

  # --- Within-country migration / place of work -----------------------------
  # Needed by the Multnomah county-tax model (migration flows). Availability is
  # partial (county-level fields especially); pruning handles gaps per year.
  - MIGRATE1
  - MIGRATE1D        # detailed migration status (dq flag below)
  - MIGPLAC1         # state/country of residence 1 year ago (dq flag below)
  - MIGCOUNTY1       # county of residence 1 year ago (partial coverage)
  - PWCOUNTY         # place-of-work county (partial coverage)
  - PWSTATE2         # place-of-work state

# ---- Data-quality flags -----------------------------------------------------
# Variables to request WITH their IPUMS data-quality (allocation) flags, so
# downstream code can screen imputed values. A name here must also appear in
# `variables` above.
data_quality_flags:
  - EMPSTAT
  - EMPSTATD
  - CLASSWKR
  - WORKEDYR
  - WKSWORK2
  - UHRSWORK
  - HHINCOME
  - FTOTINC
  - INCTOT
  - INCWAGE
  - INCEARN
  - INCBUS00
  - INCINVST
  - INCSS
  - INCWELFR
  - INCSUPP
  - INCRETIR
  - INCOTHER
  - FOODSTMP
  - RENT
  - VALUEH
  - PROPTX99
  - MIGRATE1D        # migration flags requested by the Multnomah model
  - MIGPLAC1
  # Folded in with the shelter/dwelling block, 2026-09-04. OWNCOST and RENTGRS
  # have no flag in us2006a, and ACREHOUS none in us2024c; the per-sample
  # pruning drops those and records them in that year's manifest under
  # dropped_dq_flags. PLUMBING is deliberately absent: it has no data-quality
  # flag in ANY sample probed (us2006a / us2024a / us2024c), so requesting one
  # would only add a guaranteed prune to every year.
  - OWNERSHP
  - RENTGRS
  - OWNCOST
  - MORTGAGE
  - MORTAMT1
  - MORTAMT2
  - PROPINSR
  - CONDOFEE
  - MOBLHOME
  - COSTELEC
  - COSTGAS
  - COSTWATR
  - COSTFUEL
  - FUELHEAT
  - GRADEATT
  - ROOMS
  - BEDROOMS
  - UNITSSTR
  - BUILTYR2
  - MOVEDIN
  - KITCHEN
  - ACREHOUS
  - RENTMEAL
  - VEHICLES

# ---- Case selections --------------------------------------------------------
# Restrict records by variable code, e.g. GQ: ["1","2"] to keep only
# households + additional households. DELIBERATELY EMPTY: a *common* source
# keeps every record (incl. group quarters); downstream projects filter as
# needed. Add entries only if you want the shared file itself subset.
#   gq: ["1", "2"]
case_selections: {}

# ---- Output / storage -------------------------------------------------------
output:
  # Base directory for this dataset in the shared raw_data area. Named runs sit
  # directly under it, at  <save_location>/<id>/  (no version layer). Any legacy
  # <save_location>/v1/ is left untouched alongside them.
  save_location: /nfs/roberts/project/pi_nrs36/shared/raw_data/ACS

  # RUN ID — REQUIRED. Names this pull; becomes the run folder <save_location>/<id>/.
  # Give each pull a specific, meaningful id (an empty id is a hard error). Re-running
  # with the same id is safe: existing output is skipped unless --overwrite (and in
  # per_year mode, only missing years are added). Use a new id for a fresh pull.
  # Bumped from acs_common on 2026-09-04 when the shelter and housing merge
  # layers were folded in (docs/consolidation_plan.md). A NEW id rather than
  # --overwrite: downstream repos read acs_common BY PATH, so overwriting would
  # change data underneath running projects. Each consumer re-points one line
  # and can roll back by editing it.
  id: acs_common_v2

  # LAYOUT — how the samples are packaged on disk:
  #   pooled   (default) all `samples` go into ONE extract / manifest, written
  #                      straight into the run folder <save_location>/<id>/.
  #                      Guarantees a single uniform schema across years; adding a
  #                      year means re-pulling the whole extract.
  #   per_year           one extract / folder / manifest PER sample, nested under
  #                      the run folder as <save_location>/<id>/<sample>/ (e.g.
  #                      .../acs_common/us2019a/). Each year is immutable and a
  #                      re-run (same id) pulls only missing years, so the annual
  #                      refresh is cheap. Every year is requested with the SAME
  #                      variable list, and a schema guard checks that the per-year
  #                      files stay uniformly stackable.
  layout: per_year

  # Filename prefix for the data + codebook files. Files are named
  # <data_prefix>_<sample-year> in per_year mode (e.g. usa_2020a.dat.gz / .xml)
  # and <data_prefix>_<id> in pooled mode. The IPUMS extract/submission number is
  # NOT in the filename — it is recorded in each manifest.json (ipums_extract_number).
  data_prefix: usa

  # STORAGE FORMAT.
  # keep_fixed_width: keep IPUMS's native gzipped fixed-width .dat.gz + DDI .xml.
  #   This is the recommended default: it is the most compact on-disk form AND
  #   the DDI .xml is a complete, self-documenting codebook (labels, formats,
  #   value labels). It is a big space win over the legacy v1 uncompressed CSV
  #   (~1.3 GB -> a fraction of that gzipped).
  keep_fixed_width: true
  # write_parquet: additionally write a columnar parquet PER folder for fast
  #   downstream reads (needs the `arrow` R package). Off by default to avoid the
  #   dep and the extra disk. Turn on if analysts will read this repeatedly.
  write_parquet: false
  # pooled_parquet: per_year layout ONLY. Additionally append each year into a
  #   single partitioned parquet dataset at <version>/pooled/ (year=YYYY/...), a
  #   derived convenience layer for cross-year (panel) reads with partition
  #   pruning. Appended one year at a time so memory stays bounded; needs `arrow`.
  #   Read it back with arrow::open_dataset(".../v2/pooled"). Ignored when
  #   layout is pooled.
  pooled_parquet: false
  # count_records: count data rows for the manifest (streams the .dat.gz; adds a
  #   little time on large pulls). Set false to skip.
  count_records: true
